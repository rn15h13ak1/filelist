# Changelog

[Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に概ね準拠し、
[セマンティックバージョニング](https://semver.org/lang/ja/) に従います。

バージョンはコミット履歴からさかのぼって付けたもので、タグは打っていません。

## [Unreleased]

### Added

- `CHANGELOG.md` を新規作成（本ファイル）。規約 C が必須としていたが欠けていた。
  提案は `../proposals/required-files-missing.md`（提案元 `../ws-conventions`）
  - 初版から 2026-05-22 までの 27 コミットをさかのぼり、`0.1.0` / `0.2.0` / `0.3.0` の
    3 版に区切った。**日付で切ったのではなく、まとまりで切っている**
- `.gitignore` に資格情報の 7 行（`.env` / `.env.*` / `secrets.yaml` / `credentials*` /
  `*.pem` / `*.key` / `*.local.yaml`）を追加。提案は
  `../proposals/gitignore-credential-lines.md`（提案元 `../ws-conventions`）
  - 既存の `.venv/` や `config.yaml` は仮想環境と設定ファイルで、**環境変数ファイルとは
    別物**になる。この取り違えが入っていなかった理由と考えられるため、欄のコメントに明記した
  - 検証: `git check-ignore -v` で 6 件とも該当行を示し、`.env.*` も `.env.local` で効いた。
    新しい行に当たる追跡済みファイルは 0 件で、追跡対象は減っていない

### Changed

- `~/ws` 配下の共通規約（`../ws-conventions`）への参照を `CLAUDE.md` に置いた。
  Git の取り扱い・個人情報の扱い・リポジトリの作り・編集後の検査が、本リポジトリにも適用される

### Fixed

- **個人情報の検査が 8 件の指摘を出し続けていた。** 規約 D は「編集のたびに実行する」と
  定めているため、指摘が出たままコミットする状態が常態化していた。提案は
  `../proposals/privacy-check-noise.md`（提案元 `../ws-conventions`）
  - 指摘はすべて説明とテストのための**架空のパス**で、個人情報の混入ではなかった。
    消すと説明にならない、あるいはテストが成立しないため、行末に検査の除外指定を書いた
  - **8 件の指摘に対し、印を付けたのは 7 行**になる。1 行に 2 件の検出が重なっていたため
  - 対象は `README.md` / `config.sample.yaml` / `tests/test_scanner.py`
  - 検証: 検査は「除外指定 7 行 — 問題なし」と報告する。**逃げ道を使ったことが出力から
    読み取れる**ため、黙って消えるわけではない。テスト 152 件は引き続き全パス

## [0.3.0] - 2026-05-22

大量データ（8 万件規模）でテーブルビューが実用に耐えるようになった版。

### Added

- 除外パターンの一覧をモーダルで表示できるようにした。各パターンが実際に何件に当たったかを
  **フォルダ / ファイル別の内訳付き**で出す。ヒット 0 件のパターン（実質的に使われていないもの）も
  見分けられる
  - フォルダ単位の除外は `items` に残るため画面側で数えられるが、**ファイル単位の除外は
    `items` から消える**ため、スキャナ側で数えて HTML に埋め込んでいる
- 表示件数をコントロール行の右端に常駐表示するようにした。フィルタ未適用なら全件の内訳、
  適用中は一致件数を強調して出す
- `output.table_display_limit`（任意）。フィルタで絞り込んだ結果がこの件数以下のときだけ
  テーブル本体を描画する。超過時は理由を書いた通知だけを出す
  - 巨大データで「テーブルに切り替えた瞬間に固まる」ことへの歯止めになる。
    未指定・`null`・0 以下なら無制限で、**ツリー表示は対象外**

### Changed

- **テーブルビューを仮想スクロールに切り替えた。** 可視範囲とその前後だけを DOM に置き、
  スクロールに応じて入れ替える。0.2.0 で入れたチャンク描画は**役割を終えたため外した**
  - 10,101 件での計測: テーブル切替 約 7.9ms（従来は数秒）、ソート 約 6.0ms（同）、
    DOM の行数は常に 20〜50 行（従来は全 10,101 行）
- 拡張子フィルタの選択肢を **5 文字以下の拡張子のみ**に絞った。タイムスタンプ付きの
  「なんちゃって拡張子」で一覧が埋まるのを防ぐため
  - 副作用として `tar.gz`（6 文字）も選択肢から外れる。ファイル自体は一覧にも CSV にも出る
- 祖先と子孫の関係にあるターゲットを 1 つのツリーに統合するようにした。
  上位フォルダがアクセス不可でも、その配下を明示的に指したターゲットがあれば下にぶら下がる
  - 統合先の祖先がアクセスエラーの場合、**「不可も表示」が OFF でも隠さない**。
    隠すと、見えている子孫ごと消えてしまうため
- テーブルの列幅を `<colgroup>` で固定した（`table-layout: fixed`）。ブラウザが全行を見て
  列幅を決める処理が不要になる
- 操作ボタン（親 / パス / 詳細）を行のホバー時に作るようにした。8 万行ぶんを最初に作ると、
  ボタンとイベントの登録だけで重くなるため

### 試して採らなかったもの

- `content-visibility: auto` で画面外の行の描画を省く案は**断念した。** 表の行
  （`display: table-row`）では要素の高さが 0 に潰れ、行そのものが描画されなくなる。
  CSS だけで済む見込みだったが、表には効かない

## [0.2.0] - 2026-05-21

対象パスの指定と、走査結果の見せ方を整えた版。

### Added

- `targets.path` で glob（`*` / `?` / `[...]`）を展開できるようにした。年度別フォルダのように
  同じ構造が並ぶ場合に 1 行で書ける。マッチ 0 件はエラーにして、書き間違いを早く気づけるようにした
  - `copy_as` を指定する場合は同じ位置に同じ glob が要る。捕捉した値がそこへ順に入る
- `output.path` をリストでも受け取るようにした。同じ内容を「常に最新」と「履歴」の
  2 か所へ同時に書ける
- 除外パターンに当たったフォルダを、**消さずに「除外」印を付けて残す**ようにした。
  従来は消えていたため、**除外されたのか元から無いのかが区別できなかった**
  - 既定では隠し、「除外も表示」で出す。ファイルは従来どおり完全に隠す
- 複数のターゲットが同じドライブや共有を指す場合、共通の親パスで 1 つのツリーに束ねるようにした

### Changed

- アクセス不可のフォルダを既定で隠すようにした（「不可も表示」で出せる）
- 対象パスの一覧とアクセスエラーの一覧を、画面下部の常駐表示からモーダルへ移した。
  対象を大量に指定すると、見たい中身が下へ押し出されていたため
- 最上位パス・対象パス一覧・エラー一覧を、すべて並べ替えて表示するようにした
- 検索を、入力のたびではなく**検索ボタンと Enter のときだけ**走らせるようにした。
  件数が多いと 1 文字ごとの再計算が重くなるため
- ヘッダーの縦幅を詰め、一覧の表示領域を広げた
- コピーする値は、**半角スペースを含むときだけ**引用符で囲むようにした。
  常に囲むと、表計算や文書へ貼ったときに余計な引用符が残るため

### Fixed

- アクセス不可フォルダの非表示を入れた際に、**初期表示が数秒から数十秒固まる回帰**を出していた。
  絞り込みの判定に混ぜたため、大多数の項目が「一致」扱いとなり、ツリー全体を実体化していた
  - 表示するかどうかは CSS の一括切り替えに寄せ、絞り込みの判定からは外した。
    切り替えは件数に依存しなくなった
- CSV で、未走査のフォルダのアイテム数に `0` が出ていたのを空欄にした。
  数えていないことと、数えて 0 だったことは違う

### 後から分かったこと

- 22 万件規模での「ページが応答しません」に対してテーブル描画の遅延・チャンク化を入れたが、
  **利用者の環境で実際に効いていたのはブラウザのキャッシュの残りだった。** 見立ては外れていた
  - チャンク化自体は予防として残したが、0.3.0 の仮想スクロールで置き換わっている

## [0.1.0] - 2026-05-20

初版。ローカルおよび共有ファイルサーバを再帰走査し、検索・絞り込み・パスのコピーができる
自己完結 HTML を生成する。閲覧側に Python は要らない。

### Added

- 設定ファイル（YAML）で複数のターゲットを指定できる。重なる場合はマージして重複を排除し、
  完全に同じパスはエラーにする
- `copy_as` で、走査するパスとコピーされるパスを分けられる。ドライブ割り当てで走査して
  UNC で配る、といった使い分けができる
- `max_depth` で深さを制限できる。**未走査であることを画面に明示する**ため、
  「空のフォルダ」と混同しない
- 除外パターン（ワイルドカード）
- ツリーとテーブルの 2 つのビュー、検索、拡張子・種別による絞り込み、列ソート、詳細モーダル
- シンボリックリンクは**辿らない**。リンクループと権限を越えた参照を防ぐため、
  1 行の項目として記録し、リンク先を併記する
- アクセスエラーは、行・詳細モーダル・画面上部のバナーの 3 か所で示す
- CSV エクスポート（現在の絞り込み結果のみ。Excel 向けに BOM 付き）
- 表示する列の切り替え、深さを指定した一括展開、URL のハッシュによる状態の保持
- pytest による検査（初版 99 ケース）
